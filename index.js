
import { NativeModules } from 'react-native';
import { requireNativeComponent } from 'react-native';

const { RNDeliverer } = NativeModules;

export default RNDeliverer;
export default RNCameraView;
